---
openapi: 3.0.1
info:
  title: API V1
  version: v1
paths:
  "/gameworlds":
    get:
      summary: list gameworlds
      responses:
        '200':
          description: successful
    post:
      summary: create gameworld
      responses:
        '200':
          description: successful
  "/gameworlds/{id}":
    parameters:
    - name: id
      in: path
      description: id
      required: true
      schema:
        type: string
    get:
      summary: show gameworld
      responses:
        '200':
          description: successful
servers:
- url: https://{defaultHost}
  variables:
    defaultHost:
      default: www.example.com
